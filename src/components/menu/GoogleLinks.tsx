import { IconBrandGoogleFilled } from '@tabler/icons-react';
import { IconButtonPlus } from '../button';

const GoogleLinks = () => {
  // TODO

  return (
    <IconButtonPlus tooltip="Google links">
      <IconBrandGoogleFilled />
    </IconButtonPlus>
  );
};

export default GoogleLinks;
